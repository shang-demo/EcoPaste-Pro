name: Build on Sync

on:
  workflow_call:
  workflow_dispatch:
    inputs:
      create_release:
        description: '创建 GitHub Release'
        required: false
        type: boolean
        default: true

jobs:
  build-windows:
    runs-on: windows-latest
    permissions:
      contents: write
    
    env:
      CI: false
      # 注意：如需启用自动更新签名，请在仓库 Secrets 中配置 TAURI_PRIVATE_KEY 和 TAURI_KEY_PASSWORD
      # TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_PRIVATE_KEY }}
      # TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_KEY_PASSWORD }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Get version info
        id: version
        shell: bash
        run: |
          VERSION=$(node -p "require('./package.json').version")
          FULL_VERSION="${VERSION}.${{ github.run_number }}"
          GIT_HASH=$(git rev-parse --short HEAD)
          GIT_DESC="v${FULL_VERSION}-${GIT_HASH}"
          # GitHub Actions 运行在 UTC 环境，手动加 8 小时
          BUILD_TIME=$(date -u -d '+8 hours' +'%Y-%m-%d %H:%M:%S UTC+8')
          echo "version=$FULL_VERSION" >> $GITHUB_OUTPUT
          echo "git_desc=$GIT_DESC" >> $GITHUB_OUTPUT
          echo "build_time=$BUILD_TIME" >> $GITHUB_OUTPUT
          echo "file_prefix=EcoPaste_${FULL_VERSION}" >> $GITHUB_OUTPUT

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: latest

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable

      - name: Rust cache
        uses: swatinem/rust-cache@v2
        with:
          workspaces: "./src-tauri -> target"

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build Windows app (x64)
        run: pnpm tauri build --target x86_64-pc-windows-msvc

      - name: Rename artifacts
        shell: bash
        run: |
          mkdir -p dist
          cp target/x86_64-pc-windows-msvc/release/bundle/nsis/*.exe "dist/${{ steps.version.outputs.file_prefix }}_x64-setup.exe"

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: windows-x64-build
          path: dist/*.exe
          retention-days: 7
          compression-level: 0

      - name: Create Release
        if: github.event.inputs.create_release != 'false'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ steps.version.outputs.version }}
          name: "EcoPaste_v${{ steps.version.outputs.version }}"
          body: |
            ## EcoPaste Plus 增强版
            
            基于官方 EcoPaste 的增强 Fork 版本，包含以下改进：
            
            ### ✨ 增强功能
            - **独立群组与颜色识别**：新增 "Links"、"Colors"、"Code" 群组分类。精准提取并高亮显示 RGB/RGBA 等颜色格式，路径链接高亮直达
            - **丰富二次编辑能力**：支持图片等富内容的独立二级弹窗编辑与直接文件定位。
            - **高亮代码语法预览**：自动侦测复制的代码片段并赋予原生语法高亮渲染。
            - **数据开源与显示极限扩展**：提供来源 App 获取提示框，扩容各项数据偏好设置的显示能力上限（支持配置 1~50 行）
            - **文本/图片显示限制**：支持灵活调整文本行数以及图片高度，超过后展开内容或统一收起
            - **系统截屏完美转发**：拦截并重构 SQLite 日志行为，让图片可完美保存至您指定的自定义硬盘路径，杜绝 C 盘资源浪费
            
            ### 🐛 原版修复
            - **剪贴板内容类型判断**：修复复制图片时可能由于 `html` 权重被错误拦截识别为文件或网页等一系列错误类型的问题
            - **自定义图片夹保存失败**：修正内置库 `fullName` 接口短点导致自建目录崩溃且页面显示图片红叉的严重错误
            - **数据备份入口缺失**：重现展示备份功能，确保数据留存恢复完整可用
            
            ### 📋 版本信息
            - **版本**: ${{ steps.version.outputs.version }}
            - **构建时间**: ${{ steps.version.outputs.build_time }}
            - **Git 描述**: ${{ steps.version.outputs.git_desc }}
            
            ### ⚠️ 注意事项
            - 此版本禁用了自动更新功能，避免与官方版本冲突
            - 配置文件与官方版本兼容，可直接替换使用
          draft: false
          prerelease: true
          files: dist/*.exe
        env:
          GITHUB_TOKEN: ${{ secrets.PAT_TOKEN }}
