name: Build on Sync

on:
  workflow_call:
  workflow_dispatch:
    inputs:
      create_release:
        description: '创建 GitHub Release'
        required: false
        type: boolean
        default: true

jobs:
  build-windows:
    runs-on: windows-latest
    permissions:
      contents: write
    
    env:
      CI: false
      # 注意：如需启用自动更新签名，请在仓库 Secrets 中配置 TAURI_PRIVATE_KEY 和 TAURI_KEY_PASSWORD
      # TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_PRIVATE_KEY }}
      # TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_KEY_PASSWORD }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Get version info
        id: version
        shell: bash
        run: |
          VERSION=$(node -p "require('./package.json').version")
          GIT_DESC=$(git describe --long --tags --always --dirty | sed 's/-g/-/')
          BUILD_TIME=$(TZ='Asia/Shanghai' date +'%Y-%m-%d %H:%M')
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "git_desc=$GIT_DESC" >> $GITHUB_OUTPUT
          echo "build_time=$BUILD_TIME (Beijing Time)" >> $GITHUB_OUTPUT
          echo "file_prefix=EcoPaste_${VERSION}" >> $GITHUB_OUTPUT

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: latest

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable

      - name: Rust cache
        uses: swatinem/rust-cache@v2
        with:
          workspaces: "./src-tauri -> target"

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build Windows app (x64)
        run: pnpm tauri build --target x86_64-pc-windows-msvc

      - name: Rename artifacts
        shell: bash
        run: |
          mkdir -p dist
          cp target/x86_64-pc-windows-msvc/release/bundle/nsis/*.exe "dist/${{ steps.version.outputs.file_prefix }}_x64-setup.exe"

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: windows-x64-build
          path: dist/*.exe
          retention-days: 7
          compression-level: 0

      - name: Update README download link
        shell: bash
        env:
          HUSKY: '0'
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          
          VERSION="${{ steps.version.outputs.version }}"
          
          # 使用 node 替换，并保持原始行尾格式
          node -e "
            const fs = require('fs');
            const buf = fs.readFileSync('README.md');
            let content = buf.toString('utf8');
            const hasCRLF = content.includes('\r\n');
            const v = '${VERSION}';
            const newLink = '最新版本: [EcoPaste_' + v + '](https://github.com/EcoPasteHub/EcoPaste/releases/download/v' + v + '/EcoPaste_' + v + '_x64-setup.exe)';
            content = content.replace(/^最新版本: .*/m, newLink);
            if (hasCRLF) content = content.replace(/(?<!\r)\n/g, '\r\n');
            fs.writeFileSync('README.md', content);
          "
          
          # 使用 git diff --cached 检测真正的内容变更（排除 CRLF 假差异）
          git add README.md
          if ! git diff --cached --quiet README.md; then
            git commit --no-verify -m "docs: auto update download link to v${VERSION} [skip ci]"
            git push
          else
            echo "README.md already up to date, skipping commit"
            git reset HEAD README.md 2>/dev/null || true
          fi

      - name: Create Release
        if: github.event.inputs.create_release != 'false'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ steps.version.outputs.version }}
          name: "v${{ steps.version.outputs.version }}"
          body: |
            ## EcoPaste Plus 增强版
            
            基于官方 EcoPaste 的增强 Fork 版本，包含以下改进：
            
            ### ✨ 增强功能
            - **文本显示行数可配置**：可设置剪贴板列表中纯文本、富文本、HTML的显示行数（1-20 行）
            - **图片显示高度可配置**：可设置图片预览的最大高度（50-500px）
            - **展开/收起功能**：内容超出显示范围时，可点击展开查看完整内容
            - **窗口跟随光标位置**（Windows）：窗口可跟随输入光标位置显示
            
            ### 📋 版本信息
            - **版本**: ${{ steps.version.outputs.version }}
            - **构建时间**: ${{ steps.version.outputs.build_time }}
            - **Git 描述**: ${{ steps.version.outputs.git_desc }}
            
            ### ⚠️ 注意事项
            - 此版本禁用了自动更新功能，避免与官方版本冲突
            - 配置文件与官方版本兼容，可直接替换使用
          draft: false
          prerelease: true
          files: dist/*.exe
        env:
          GITHUB_TOKEN: ${{ secrets.PAT_TOKEN }}
